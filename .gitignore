node_modules/
.DS_Store
*.bak
.vercel
*.xlsx
.~lock.*

# One-shot admin DB migration script — unauthenticated endpoint using the Supabase
# service-role key; kept locally only, never committed/deployed. See file header.
api/admin/run-pk-migration.js

# One-off image extraction scratch scripts (course catalog build) — not part of the product
_dedup_images.py
_extract_brochure.py
_extract_images.py
_extract_yours.py
_make_thumbs.py
