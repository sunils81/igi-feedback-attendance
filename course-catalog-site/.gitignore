.vercel
.env*

# Reference PDFs — large (400MB+), never needed in git, kept locally only
source-docs/

# Pre-dedup image extraction output (133 files) — assets/images/unique/ has the real set
assets/images/raw-extracted/

# Local-only preview thumbnails, not used by the live site
assets/images/_thumbs_for_review/
