diff --git a/instructor-portal.html b/instructor-portal.html
index 66edb91..c8dd715 100644
--- a/instructor-portal.html
+++ b/instructor-portal.html
@@ -34,6 +34,19 @@
 .progress-fill{height:6px;background:var(--gold);border-radius:3px;transition:width .5s}
 .grid2{display:grid;grid-template-columns:1fr 1fr;gap:12px}
 @media(max-width:500px){.grid2{grid-template-columns:1fr}}
+
+.day-batch-card{background:var(--off);border-radius:10px;padding:16px;margin-bottom:14px;border:1.5px solid var(--border)}
+.day-batch-card .dbc-title{font-weight:700;font-size:14px;color:var(--navy)}
+.day-batch-card .dbc-meta{font-size:11px;color:var(--muted);margin-top:2px;margin-bottom:12px}
+.day-badge{display:inline-block;padding:3px 10px;border-radius:10px;font-size:11px;font-weight:600;margin-right:6px}
+.day-badge-first{background:#EEF4FB;color:var(--blue)}
+.day-badge-second{background:var(--gold-pale);color:#B87A10}
+.day-badge-full{background:#E8F5EE;color:#1a7a3c}
+.day-sel{width:100%;border:1.5px solid var(--border);border-radius:8px;padding:9px 12px;font-family:'DM Sans',sans-serif;font-size:13px;background:var(--white);color:var(--navy);outline:none;margin-bottom:8px}
+.day-sel:focus{border-color:var(--gold)}
+.topic-field{width:100%;border:1.5px solid var(--gold);border-radius:8px;padding:9px 12px;font-family:'DM Sans',sans-serif;font-size:13px;background:var(--gold-pale);color:var(--navy);outline:none;margin-bottom:8px}
+.topic-override{width:100%;border:1.5px solid var(--border);border-radius:8px;padding:8px 12px;font-family:'DM Sans',sans-serif;font-size:12px;background:var(--white);color:var(--navy);outline:none;margin-bottom:10px}
+.topic-override:focus{border-color:var(--gold)}
 </style>
 </head>
 <body>
@@ -82,27 +95,42 @@
     <button class="tab" onclick="iTab('t-mybatches')">🗂 My Batches</button>
   </div>
 
-  <!-- ── TAB 1: CREATE SESSION ── -->
+  <!-- ── TAB 1: TODAY ── -->
   <div id="t-session" class="tab-content active">
+    <!-- Today's auto-sessions per batch -->
     <div class="card">
-      <div class="section-tag">Today's Session</div>
-      <h2>Create Session</h2>
-      <p class="sub">Select your batch, set the date, and create today's session. The system generates a session code and QR code automatically.</p>
+      <div class="section-tag">Today's Sessions</div>
+      <h2 id="today-title">—</h2>
+      <p class="sub">Sessions for today are created automatically. You can update the topic if needed or add an extra session.</p>
+      <div id="today-batches-wrap">
+        <div class="spinner-wrap"><div class="spinner"></div><p>Loading your batches…</p></div>
+      </div>
+    </div>
+
+    <!-- Extra session card (only for non-scheduled days) -->
+    <div class="card" id="extra-sess-card">
+      <div class="section-tag">Extra / Extension Session</div>
+      <h2>Create Extra Session</h2>
+      <p class="sub">Use this only for 2nd/4th Saturday makeup sessions or sessions after the batch end date.</p>
       <div class="field"><label>Select Batch *</label>
-        <select id="ss-batch" onchange="onBatchSelect()"><option value="">Select a batch</option></select>
+        <select id="ex-batch"><option value="">Select a batch</option></select>
       </div>
       <div class="field"><label>Session Date *</label>
-        <input type="date" id="ss-date">
+        <input type="date" id="ex-date">
+      </div>
+      <div class="field"><label>Topic *</label>
+        <input type="text" id="ex-topic" placeholder="Describe what will be covered…">
       </div>
-      <div class="info-box" id="ss-topic-info" style="display:none"></div>
-      <button class="btn btn-gold" id="ss-btn" onclick="createSession()">Create Session →</button>
-      <div class="err-box" id="ss-err"></div>
+      <button class="btn btn-gold" id="ex-btn" onclick="createExtraSession()">Create Extra Session →</button>
+      <div class="err-box" id="ex-err"></div>
     </div>
+
+    <!-- Session result -->
     <div class="card" id="ss-result" style="display:none">
-      <div class="section-tag">Session Created ✓</div>
+      <div class="section-tag">Session Ready ✓</div>
       <h2 id="ss-code">—</h2>
-      <p class="sub" id="ss-type-disp">—</p>
-      <div class="field"><label>Student Feedback Link</label>
+      <p class="sub" id="ss-type-disp">Students can submit via the Student Portal — no link needed.</p>
+      <div class="field"><label>Optional: Share direct link</label>
         <div style="display:flex;gap:8px;align-items:center">
           <input type="text" id="ss-link" class="auto-val" readonly style="flex:1;font-size:12px">
           <button class="btn btn-outline" style="width:auto;padding:10px 12px" onclick="copyLink()">📋</button>
@@ -246,6 +274,7 @@ document.addEventListener('DOMContentLoaded', function() {
   });
   document.getElementById('ss-date').value = new Date().toISOString().split('T')[0];
   document.getElementById('ct-date').value = new Date().toISOString().split('T')[0];
+  document.getElementById('ex-date').value = new Date().toISOString().split('T')[0];
   document.getElementById('lg-pin').addEventListener('keydown', e => { if(e.key==='Enter') doLogin(); });
 });
 
@@ -279,6 +308,7 @@ function doLogin() {
     document.getElementById('portal-name').textContent = name;
     showScreen('s-main');
     loadInstructorBatches();
+    loadTodaySessions();
   });
 }
 
@@ -286,7 +316,7 @@ function loadInstructorBatches() {
   gasGet({action:'getInstructorBatches', instructor:instructorName}, function(e,d) {
     if (e||!d||!d.batches) return;
     const batches = d.batches;
-    ['ss-batch','at-batch','mk-batch'].forEach(id => {
+    ['at-batch','mk-batch','ex-batch'].forEach(id => {
       const sel = document.getElementById(id);
       sel.innerHTML = '<option value="">Select a batch</option>';
       batches.forEach(b => {
